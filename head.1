


HEAD(1)        Human68k ITA TOOLBOX User's Manual         HEAD(1)



名称
     head - ファイルの先頭部分を出力する

形式
     head [ -FZh ] [ -count ] [ file [ -count ] ] ...

解説
     head は，各 file を順次読み取り，それぞれの先頭の count 数だ
     けの行を標準出力に出力します．count は10進整数です．count が
     指定されない場合のデフォルトは 10 です．

     file が 1つも指定されていない場合には標準入力から読み取りを
     行います．

     file がオープンできないときにはエラー・メッセージを標準エラー
     出力に出力して処理を中止します．

     最初の file の後ろにさらに引数が続いている場合には，次のよう
     に各 file の内容の前に‘==> file <==’という形の行が出力され
     ます．（% で始まる行はシェルの入力行です）

          % head -1 file1 file2
          ==> file1 <==
          これは file1 の 1行目です．

          ==> file2 <==
          これは file2 の 1行目です．
          %

     このように，ファイルとファイルの間には 1行の空行も出力されま
     す．

     ファイル毎に別々の count を指定することができます．例を示し
     ます．

          % head -1 file1 -2 file2
          ==> file1 <==
          これは file1 の 1行目です．

          ==> file2 <==
          これは file2 の 1行目です．
          これは file2 の 2行目です．
          %

     指定 file が 1つの場合，通常は次のように file の内容だけが出
     力されます．

          % head -1 file1
          これは file1 の 1行目です．
          %

     しかしこの場合でも，次のようにすれば‘==> file <==’行を出力
     させることが可能です．

          % head -1 file1 -0
          ==> file1 <==
          これは file1 の 1行目です．
          %

     入力が cooked モードのキャラクタ・デバイス（CON など）である
     場合，^D (control-D) かまたは ^Z を読み取ると，それを EOF コー
     ドと見なして入力を打ち切ります．

     標準出力が cooked モードのキャラクタ・デバイスである場合には，
     通常，単体の ^J コードを ^M^J に変換して出力します．

     head に指定できるオプションを以下に示します．

     -F   いかなる場合にも ^J コードを変換しません．

     -Z   いかなる場合にも ^Z コードを EOF と見なして入力を打ち切
          ります．

     -h   ‘==> file <==’行や空行を付加出力しません．純粋にファ
          イルの内容だけが出力されます．

     これらのオプションは -F -Z のようにそれぞれ単独に記述するこ
     ともできますし，-FZ のように連結して指定することもできます．

診断
     何らかのエラーがあったときにはエラー・メッセージが標準エラー
     出力に出力されます．

     終了ステータスとしては，引数の記述に間違いがあった場合には 1，
     ファイルがオープンできなかったり入力に失敗した場合には 2，出
     力エラーやメモリ不足などの深刻なエラーが起こった場合には 3，
     すべてが正常に終了したならば 0 が返ります．

不具合
     cooked モードのキャラクタ・デバイスからは，データとしての ^D
     および ^Z を入力することはできません．

     CON からは行単位で入力されますので，^D あるいは ^Z の後にも
     改行が必要です．

関連事項
     cat(1), more(1), tail(1), tee(1)

制作・著作
     Copyright(C)1991 by 板垣 史彦 (Itagaki Fumihiko)



1.0                 Last change: 15 Aug 1992     Itagaki Fumihiko



